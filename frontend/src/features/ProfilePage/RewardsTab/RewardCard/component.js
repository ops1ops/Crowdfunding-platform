import React from 'react';
import { PropTypes } from 'prop-types';

const RewardCard = props => {
    return <div>qweqe</div>;
};

RewardCard.propTypes = {
    reward: PropTypes.object.isRequired,
};

export default RewardCard;
